# Keyboard mapping

# Move around
map n scroll down
map e scroll up
map i scroll right
map o scroll left

# Scroll more
map <C-n> scroll half-down
map <C-e> scroll half-up

# Zoom
map t zoom in
map s zoom out
map r adjust_window best-fit
map w adjust_window width

# Find
map m search forward
map M search backward

# Other settings
set selection-clipboard clipboard
set recolor true

# Color scheme
include ./colors
