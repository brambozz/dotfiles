#!/bin/sh

# A simple rofi shutdown prompt
# To be riced, but it is at least functional

# I could make this better by not using rofi, but rather just display
# an image displaying the various options with their key characters.
# Then read a single character with `read -n1 answer` and enter
# the if statements below.

choice=$(echo -e \
"a) Poweroff\n\
b) Hibernate\n\
c) Suspend\n\
d) Reboot" | \
rofi -dmenu -format 'i' -location 1 -width 100 -lines 4 -p "Shutdown?" -i)

# If choice == 0; shutdown
if [ $choice -eq 0 ]
then
	systemctl poweroff
fi
# If choice == 1; hibernate
if [ $choice -eq 1 ]
then
	systemctl hibernate
fi
# If choice == 2; sleep
if [ $choice -eq 2 ]
then
	systemctl suspend
fi
# If choice == 3; restart
if [ $choice -eq 3 ]
then
	systemctl reboot
fi
